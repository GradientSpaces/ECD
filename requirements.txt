# Core
# For GPU/CUDA, install torch FIRST from the CUDA index (see README "Installation");
# installing it via this file alone may pull a CPU-only wheel.
torch>=2.8.0
einops>=0.8.2
timm>=0.9.0
numpy>=1.23
h5py>=3.8
matplotlib>=3.6
pandas>=1.5
tqdm
wandb
# OGBench + MuJoCo
ogbench>=1.0
mujoco>=3.1.6,<3.4  # for cp39 compatibility
# Ipython for the demo notebook
ipython
ipykernel